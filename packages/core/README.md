# smart-fetch-core

Internal shared core for the `pi-smart-fetch` and `openclaw-smart-fetch` packages.

It contains the reusable fetch/extract pipeline, shared request schema helpers, and shared response formatting.
